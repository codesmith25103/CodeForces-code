#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    cin.tie(0);cin.sync_with_stdio(0);
    cout.tie(0);cout.sync_with_stdio(0);
    int t = 1;
    cin >> t;
    while (t--)
    {
        int l, r;
        cin>>l>>r;
        if(r-l>100)
        {
            int m1=INT_MAX;
            int m2=INT_MIN;
            for(int i=l; i<=r; i++)
            {
                
            }
        }
    }
    return 0;
}