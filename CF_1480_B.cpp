#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    cin.tie(0);cin.sync_with_stdio(0);
    cout.tie(0);cout.sync_with_stdio(0);
    int t = 1;
    cin >> t;
    while (t--)
    {
        int heroLife, heroAttack, n;
        cin>>heroLife>>heroAttack>>n;
        vector<int> life(n);
        vector<int> attack(n);
        int sum1=0, sum2=0;
        for(int i=0; i<n-1; i++)
        {
            sum1+=life[i];
    }
    return 0;
}